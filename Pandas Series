'''
PANDAS SERIES:
squeeze = True changes the dataframe to a series
Some series attributes like .uniquie(); .sort_values; .sort_index; are functions of Pandas Series only. 
'''

# C:\Users\13098\Desktop\Location.csv  
import pandas as pd
with open(r'C:\Users\13098\Desktop\Location.csv') as file:
    df=pd.read_csv(file, squeeze=True) #squeeze changes dataframe to series
    print(type(df))
    #print(df.describe())
    #print(type(df))
    #print(df)
    df1=df.unique()  # Series use only, must use squeeze=True;
    print(df1)
    df=df.sort_values(ascending=True) 
    df=df.sort_index(ascending=True)
    print(df)
